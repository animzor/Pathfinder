# Pathfinder

Pathfinder is a program that lets you create maps with custom locations. The program then allows you to find the shortest path between
locations using Dijkstras shortest path algorithm.
